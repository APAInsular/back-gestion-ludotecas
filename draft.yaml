models:
  Playroom:
    name: string
    address: string
    phone: string
    email: string
    password: string


  Configuration:
    language: string
    notifications: boolean
    user_id: foreign

  Role:
    name: string

  Forum:
    title: string
    description: text

  Group:
    name: string
    description: text

  Employee:
    user_id: foreign
    position: string

  Incidence:
    description: text
    date: dateTime
    employee_id: foreign

  Administrator:
    user_id: foreign

  Tutor:
    user_id: foreign

  Kid:
    name: string
    birthdate: date
    playroom_id: foreign
    tutor_id: foreign

  Booking:
    date: dateTime
    status: string
    kid_id: foreign

  Service:
    name: string
    description: text
    playroom_id: foreign

  Activity:
    name: string
    date: dateTime
    hour: time
    description: text
    service_id: foreign

  Announcement:
    title: string
    description: text
    playroom_id: foreign

  Menu:
    title: string
    description: text
    playroom_id: foreign

  Event:
    title: string
    date: dateTime
    hour: time
    description: text
    location: string
    playroom_id: foreign

controllers:
  Playroom:
    resource: api

  User:
    resource: api

  Configuration:
    resource: api

  Role:
    resource: api

  Forum:
    resource: api

  Group:
    resource: api

  Employee:
    resource: api

  Incidence:
    resource: api

  Administrator:
    resource: api

  Tutor:
    resource: api

  Kid:
    resource: api

  Booking:
    resource: api

  Service:
    resource: api

  Activity:
    resource: api

  Announcement:
    resource: api

  Menu:
    resource: api

  Event:
    resource: api
